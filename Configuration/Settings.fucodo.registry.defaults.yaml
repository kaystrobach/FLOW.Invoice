# add defaults here with the namespace key and then the actual key
# only comes in effect, when no value and no fallback are given
#
fucodo:
  registry:
    defaults:
      KayStrobach.Invoice.General:
        invoice.series:
          type: Textarea
          label: Rechnungskreise (Achtung, braucht JSON als Input)
          default: '[{"identifier":"default", "name":"Standard", "prefix":"R-%year-", "postfix":"", "type":"KayStrobach\\Invoice\\Domain\\Model\\Invoice", "description":"Standardrechnung, hier werden einige Werte automatisch gesetzt"}, {"identifier":"recurringBilling", "name":"Dauerrechnung", "prefix":"DR-%year-", "postfix":"", "type":"KayStrobach\\Invoice\\Domain\\Model\\Invoice", "description":"Dauerrechnung, hier werden einige Werte automatisch gesetzt, die es ermöglichen die Rechnung als Dauerrechnung zu verwenden"}]'
          description:
      KayStrobach.Invoice.NormalInvoiceSettings:
        items:
          ################################################################################################################
          # General Invoice defaults
          #################################################################################################################
          title:
            type: Text
            label:
            default: Rechnung
            description:

          subTitle:
            type: Text
            label:
            default: ""
            description:
          additionalInformation:
            type: Text
            label:
            default: |
              Für die nachfolgende aufgeführten Leistungen berechnen wir Ihnen analog zum Angebot Nr. {invoice.offerNumber}
              und den Allgemeinen Geschäftsbedingungen (Stand: Oktober 2024)
            description:
          ################################################################################################################
          # All Seller Related defaults
          ################################################################################################################
          seller.name:
            type: Text
            label:
            default: Rechnung
            description:
          seller.personName:
            type: Text
            label:
            default: Rechnung
            description:
          seller.street:
            type: Text
            label:
            default: Rechnung
            description:
          seller.houseNumber:
            type: Text
            label:
            default: Rechnung
            description:
          seller.addressAddon:
            type: Text
            label:
            default: Rechnung
            description:
          seller.roomNumber:
            type: Text
            label:
            default: Rechnung
            description:
          seller.zipCode:
            type: Text
            label:
            default: Rechnung
            description:
          seller.city:
            type: Text
            label:
            default: Rechnung
            description:
          seller.country:
            type: Text
            label:
            default: Rechnung
            description:
          seller.countryCode:
            type: Text
            label:
            default: Rechnung
            description:
          seller.vatID:
            type: Text
            label:
            default: Rechnung
            description:
          seller.email:
            type: Text
            label:
            default: Rechnung
            description:
